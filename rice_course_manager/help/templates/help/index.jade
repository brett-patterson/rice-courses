extends base

block content
    section
        each article in articles
            article(id='{{ article.title | slugify }}')
                h2 #{ article.title }
                hr
                - autoescape off
                    #{ article.render_html }
                hr
