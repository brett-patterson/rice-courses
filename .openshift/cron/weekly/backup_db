FILENAME=$OPENSHIFT_DATA_DIR/$(date).sql
FILENAME=${FILENAME//\ /_}

mysqldump -h $OPENSHIFT_MYSQL_DB_HOST -P ${OPENSHIFT_MYSQL_DB_PORT:-3306} -u ${OPENSHIFT_MYSQL_DB_USERNAME:-'admin'} --password="$OPENSHIFT_MYSQL_DB_PASSWORD" --all-databases  > $FILENAME